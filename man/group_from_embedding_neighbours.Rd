% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_occupancy_groups.R
\name{group_from_embedding_neighbours}
\alias{group_from_embedding_neighbours}
\title{Wrapper: group of the K spectrograms most similar to a reference (embedding-based)}
\usage{
group_from_embedding_neighbours(
  pool,
  project_id,
  group_name,
  target_count,
  reference_spec_id,
  top_k,
  description = NULL
)
}
\arguments{
\item{pool}{A DBI pool.}

\item{project_id}{Integer. Owning project.}

\item{group_name}{Character. Unique within project.}

\item{target_count}{Integer >= 1. Stop threshold (number of certain
detections of the queue/target species required to mark group as done).}

\item{reference_spec_id}{Integer. The "seed" spectrogram whose embedding
defines the similarity centre.}

\item{top_k}{Integer. Number of nearest neighbours to retrieve (includes
the reference itself).}

\item{description}{Optional character. Free-form note.}
}
\description{
Requires the `vector` extension and an `embedding` column on
`import.spectrograms`. Returns the K spectrograms with the smallest
cosine distance to the reference spectrogram's embedding.
}
\details{
Disabled-friendly: throws a clear error if the embedding column is missing,
so the rest of the codebase can run without pgvector installed.
}
