% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_ingest_audio_files.R
\name{ingest_audio_files}
\alias{ingest_audio_files}
\title{Ingest audio files for a project into the database}
\usage{
ingest_audio_files(
  pool,
  project_name_short,
  project_name_long = project_name_short,
  description = NULL,
  contact = NULL,
  organisation = NULL,
  project_folder,
  deployments_sf,
  folder_depth = 1L,
  force_tz = "UTC",
  force_exif = FALSE,
  list_files_retries = 3L,
  list_files_verify = TRUE,
  list_files_min_stable_scans = 2L,
  default_required_annotation_type_id = 3L,
  max_relative_path_length = 200L,
  remove_corrupt_indices = FALSE,
  exiftool_exe = NULL,
  perl_path = NULL,
  update_if_exists = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{pool}{A database connection pool (from \code{\link{set_db_pool}}).}

\item{project_name_short}{Character. Short project identifier (max 30 chars).
Used to look up or create the project in \code{import.projects}.}

\item{project_name_long}{Character. Full project name. Defaults to
\code{project_name_short}.}

\item{description}{Character or NULL. Project description.}

\item{contact}{Character or NULL. Contact person.}

\item{organisation}{Character or NULL. Organisation name.}

\item{project_folder}{Character. Root folder whose subdirectories are
deployment directories (at depth \code{folder_depth}).}

\item{deployments_sf}{An sf object (point geometry, EPSG:4326) with at least
a \code{deployment_name} column whose values match the folder names found
under \code{project_folder}. Optional extra columns accepted by
\code{import.deployments}: \code{device_manufacturer},
\code{device_modelname}, \code{notes}, \code{valid}.}

\item{folder_depth}{Integer. Directory levels below \code{project_folder} to
scan for deployments. Default 1.}

\item{force_tz}{Character. Timezone to assign to all scanned timestamps.
Default \code{"UTC"}.}

\item{force_exif}{Logical. Re-read EXIF even when an index file already
exists. Default \code{FALSE}.}

\item{list_files_retries}{Integer. Retry count for directory scans (useful
on network drives). Default 3.}

\item{list_files_verify}{Logical. Accept a directory scan only after
consecutive identical results. Default \code{TRUE}.}

\item{list_files_min_stable_scans}{Integer. Consecutive identical scans
required. Default 2.}

\item{default_required_annotation_type_id}{Integer. Annotation type assigned
to all audio files. Default 3.}

\item{max_relative_path_length}{Integer. Paths longer than this are treated
as corrupt exiftool output and excluded. Default 200.}

\item{remove_corrupt_indices}{Logical. If \code{TRUE}, deletes the .fst index
files for deployments with corrupt paths so they will be re-scanned on the
next run. Default \code{FALSE} (rows are dropped but files are kept).}

\item{exiftool_exe}{Character or NULL. Absolute path to the exiftool
executable. Required on Windows when exiftool is not on PATH. Default NULL.}

\item{perl_path}{Character or NULL. Path to Perl (Windows only, used by
exiftoolr). Default NULL.}

\item{update_if_exists}{Logical. Update existing deployment and audio_file
records on conflict. Default \code{TRUE}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Invisibly returns a named list:
  \describe{
    \item{project_id}{Integer project_id.}
    \item{deployment_ids}{Integer vector of upserted deployment_ids.}
    \item{audio_file_ids}{Integer vector of upserted audio_file_ids.}
  }
}
\description{
Orchestrates the full audio file ingestion pipeline: project upsert, local
file scanning via \code{\link{retrieve_local_file_info}}, deployment
registration, duplicate timestamp resolution, corrupt-path filtering, and
audio file upload.
}
\details{
The one project-specific task — building the deployment location table —
is left to the caller: construct an sf object with deployment names and
point geometries and pass it as \code{deployments_sf}.
}
