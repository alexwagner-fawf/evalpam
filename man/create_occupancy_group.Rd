% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_occupancy_groups.R
\name{create_occupancy_group}
\alias{create_occupancy_group}
\title{Create or update an occupancy group from explicit spectrogram IDs}
\usage{
create_occupancy_group(
  pool,
  project_id,
  group_name,
  target_count,
  spectrogram_ids,
  description = NULL
)
}
\arguments{
\item{pool}{A DBI pool.}

\item{project_id}{Integer. Owning project.}

\item{group_name}{Character. Unique within project.}

\item{target_count}{Integer >= 1. Stop threshold (number of certain
detections of the queue/target species required to mark group as done).}

\item{spectrogram_ids}{Integer vector. Spectrograms to assign to the group.
Must reference rows that exist in `import.spectrograms`.}

\item{description}{Optional character. Free-form note.}
}
\value{
The integer `group_id`.
}
\description{
Primary, lowest-level interface. All wrappers funnel into this.
}
\details{
Idempotent: re-running with the same `project_id` + `group_name` updates
`target_count` and `description`, and appends any new spectrograms to the
group (existing memberships are kept; `ON CONFLICT DO NOTHING`).
}
