% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_generate_spectrograms.R
\name{generate_spectrograms}
\alias{generate_spectrograms}
\title{Generate and upload audio clip spectrograms for a project}
\usage{
generate_spectrograms(
  pool,
  project_id = NULL,
  deployment_ids = NULL,
  species_ids = NULL,
  audio_file_ids = NULL,
  settings_ids = NULL,
  result_ids = NULL,
  result_id_mode = c("exclusive", "prioritize"),
  n_per_species = 30L,
  confidence_selection_mode = "top",
  grouping_by = c("species_id", "deployment_id"),
  padding_s = 2,
  output_dir = NULL,
  export_to_db = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{pool}{A database connection pool (from \code{\link{set_db_pool}}).}

\item{project_id}{Integer or NULL. Filter deployments to this project.
\code{NULL} uses all deployments (requires \code{deployment_ids} to be
set, otherwise processes the entire database).}

\item{deployment_ids}{Integer vector or NULL. Explicit set of deployment IDs
to process. If \code{NULL} (default), deployments are selected from the
project with one representative per geographic location (earliest start
date). Supply this to override the automatic selection entirely.}

\item{species_ids}{Integer vector or NULL. Restrict spectrogram generation
to BirdNET detections of these species. \code{NULL} (default) applies no
species constraint. Combined with \code{audio_file_ids} as a logical AND
and layered on top of the deployment selection and sampling.}

\item{audio_file_ids}{Integer vector or NULL. Restrict spectrogram
generation to detections in these audio files. \code{NULL} (default)
applies no file constraint. Files outside the selected deployments are
still excluded (the two filters intersect).}

\item{settings_ids}{Integer vector or NULL. Restrict spectrogram generation
to detections produced under these inference \code{settings_id} values.
\code{NULL} (default) pools detections from all settings. Use this to
generate clips for one specific inference run/configuration.}

\item{result_ids}{Integer vector or NULL. Specific detection \code{result_id}
values to build clips from. Interpreted per \code{result_id_mode}.
\code{NULL} (default) applies no result-id constraint.}

\item{result_id_mode}{Character, \code{"exclusive"} (default) or
\code{"prioritize"}; only relevant when \code{result_ids} is set. Passed to
\code{\link{sample_results_table}}: \code{"exclusive"} builds clips only for
the listed detections, \code{"prioritize"} builds them first and tops up
each group with other detections up to \code{n_per_species}.}

\item{n_per_species}{Integer. Number of detections to select per group
(see \code{grouping_by}). Default 30.}

\item{confidence_selection_mode}{Character. Passed to
\code{\link{sample_results_table}}. One of \code{"top"}, \code{"random"},
or \code{"stratified"}. Default \code{"top"}.}

\item{grouping_by}{Character vector. Grouping columns for
\code{\link{sample_results_table}}. Default
\code{c("species_id", "deployment_id")}.}

\item{padding_s}{Numeric. Seconds of audio context before and after the
detection window. Default 2.}

\item{output_dir}{Character or NULL. Directory for MP3 cache files. If
\code{NULL} (default), reads the \code{spectogram_folder} environment
variable, falling back to \code{./spectograms}.}

\item{export_to_db}{Logical. Upload MP3 blobs to \code{import.spectrograms}.
Default \code{TRUE}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link{build_audio_clips_db}}.}
}
\value{
Invisibly returns the number of unique clips generated.
}
\description{
Samples detection results from the database, deduplicates to one clip per
time window (keeping the highest-confidence detection as the representative),
and calls \code{\link{build_audio_clips_db}} to generate MP3 clips and
optionally upload them to \code{import.spectrograms}.
}
