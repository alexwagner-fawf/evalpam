% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_occupancy_groups.R
\name{remove_spectrogram_blobs}
\alias{remove_spectrogram_blobs}
\title{Remove only the audio blob from spectrograms}
\usage{
remove_spectrogram_blobs(
  pool,
  spectrogram_ids = NULL,
  group_ids = NULL,
  verbose = TRUE
)
}
\arguments{
\item{pool}{A DBI pool.}

\item{spectrogram_ids}{Integer vector of spectrogram ids (optional).}

\item{group_ids}{Integer vector of occupancy group ids whose member
spectrograms should be cleared (optional).}

\item{verbose}{Logical. Emit a summary message. Default \code{TRUE}.}
}
\value{
Invisibly, a list with \code{blobs_removed} (rows actually cleared)
  and \code{spectrogram_ids} (the full target set).
}
\description{
Sets \code{import.spectrograms.audio_data = NULL} for the target spectrograms,
leaving every other column (and the rows themselves, their group memberships
and annotations) intact. This frees the (out-of-line TOAST) blob storage while
keeping enough metadata to regenerate the exact clip later with
\code{\link{regenerate_spectrogram_blobs}}.
}
\details{
Targets are the union of \code{spectrogram_ids} and the member spectrograms of
\code{group_ids} (supply either or both).
}
